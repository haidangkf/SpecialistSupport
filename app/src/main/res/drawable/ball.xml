<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="oval">
    <solid
        android:color="@color/primario_2"/>
    <!-- il bordo mi fa da margine per staccarlo dalla textview-->
    <stroke
        android:width="5dp"
        android:color="@android:color/transparent"/>
    <size
        android:width="10dp"
        android:height="10dp"/>
</shape>